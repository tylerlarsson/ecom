// const HttpStatus = require('http-status-codes');
// const request = require('supertest');
// const config = require('../../server/config');
// const app = require('../../server/web-server');
// const db = require('../../server/db/test');
// const path = `${config.get('base-path')}/file`;

describe('file api test', () => {
  // POST /file/image
  test('should raise error if invalid schema');
  test('should raise error if cant recognize content type');
  test('');
  test('');

  // DELETE /file/image
  test(``);
  test(``);
  test('');
  test('');
});
